import React from 'react';
import ReactDOM from 'react-dom/client';
import ReadingTracker from './ReadingTracker.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ReadingTracker />
  </React.StrictMode>
);
